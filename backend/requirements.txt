# Kept to exactly what the live code imports -- see CLAUDE.md's "Deferred, seam-only"
# section for what's intentionally not here yet (sqlalchemy/psycopg2, redis, chromadb,
# langchain/langgraph, anthropic). Add a dependency here only when you start using it,
# and log why in Documents/11_Decision_Log.md.
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0
pandas>=2.0.0
httpx>=0.24.0
